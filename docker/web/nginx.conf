server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # Serve index.html for all requests (supports future SPA routing).
    # The wget healthcheck hits / and expects HTTP 200.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
