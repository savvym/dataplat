# ============================================================
# Dataplat dev stack — environment defaults
# Copy this file to docker/.env and fill in real secrets.
# The compose file loads this via env_file: .env.example so
# the stack works WITHOUT copying — defaults are safe for local dev.
# NEVER commit docker/.env (it is gitignored).
# ============================================================

# Postgres
POSTGRES_USER=app
POSTGRES_PASSWORD=devpassword
POSTGRES_DB=platform
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB_DAGSTER=platform_dagster

# Redis
REDIS_URL=redis://redis:6379/0

# MinIO
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=devpassword

# Dagster (used internally by FastAPI via DagsterGateway — S004-F-004)
# Renamed from DAGSTER_GRAPHQL in sprint S004-F-004 — update your local .env if you have the old name.
DAGSTER_GRAPHQL_URL=http://dagster-webserver:3000/graphql

# Auth — JWT secret key (S007-F-007)
# Change this to a strong random value in production: openssl rand -hex 32
SECRET_KEY=dev-secret-key-change-me  # Change in production

# LLM keys — not used until F-027/F-028/F-043; leave blank for F-001
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Host port mapping (container ports are unchanged from agreed.md §5).
# Default offsets to +10000 to avoid collisions with other dev stacks
# already running on this machine. Override here if needed.
POSTGRES_HOST_PORT=15432
REDIS_HOST_PORT=16379
MINIO_API_HOST_PORT=19000
MINIO_CONSOLE_HOST_PORT=19001
DAGSTER_HOST_PORT=13000
FASTAPI_HOST_PORT=18000
FRONTEND_HOST_PORT=15173
